"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var Result_1 = require("./Result");
/**
 *  Maps a `Result<T, E>` to `Result<U, E>` by applying a _selector_ function
 *  to an contained `Ok(T)` value, leaving an `Err(E)` value untouched.
 *
 *  This function can be used to compose the results of two functions.
 */
function mapForResult(src, selector) {
    if (src.ok) {
        var r = selector(src.val);
        return Result_1.createOk(r);
    }
    else {
        var s = src;
        return s;
    }
}
exports.mapForResult = mapForResult;
