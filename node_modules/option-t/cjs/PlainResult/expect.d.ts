import { Result } from './Result';
/**
 *  Return _v_ as `T` if the passed _v_ is `Ok(T)`.
 *  Otherwise, throw `TypeError` with the passed `msg`.
 */
export declare function expectIsOk<T, E>(v: Result<T, E>, msg: string): T | never;
/**
 *  Return _v_ as `E` if the passed _v_ is `Err(E)`.
 *  Otherwise, throw `TypeError` with the passed `msg`.
 */
export declare function expectIsErr<T, E>(v: Result<T, E>, msg: string): E | never;
