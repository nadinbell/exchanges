import { Option } from '../PlainOption/Option';
import { Result } from './Result';
/**
 *  Convert to `Some(T)` if _v_ is `Ok(T)`.
 *  Otherwise, return `None`.
 */
export declare function toOptionFromOk<T, E>(v: Result<T, E>): Option<T>;
/**
 *  Convert to `Some(E)` if _v_ is `Err(E)`.
 *  Otherwise, return `None`.
 */
export declare function toOptionFromErr<T, E>(v: Result<T, E>): Option<E>;
