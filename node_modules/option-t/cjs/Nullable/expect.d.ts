import { Nullable } from './Nullable';
/**
 *  Return _v_ as `T` if the passed _v_ is not `null`.
 *  Otherwise, throw `TypeError` with the passed `msg`.
 */
export declare function expectNotNull<T>(v: Nullable<T>, msg: string): T | never;
