import { andThenForOption } from './andThen';
function flatten(input) {
    return input;
}
/**
 *  Flatten the inner value of _input_.
 */
export function flattenForOption(input) {
    var r = andThenForOption(input, flatten);
    return r;
}
