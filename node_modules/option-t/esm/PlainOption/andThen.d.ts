import { MapFn } from '../shared/Function';
import { Option } from './Option';
export declare type FlatmapFn<T, U> = MapFn<T, Option<U>>;
/**
 *  Returns `None` if the _src_ is `None`,
 *  otherwise calls _selector_ with the value and returns the result.
 *
 *  XXX:
 *  Some languages call this operation flatmap.
 *  But we don't provide `flatMap()` as alias of this function
 *  to sort with other APIs.
 */
export declare function andThenForOption<T, U>(src: Option<T>, selector: FlatmapFn<T, U>): Option<U>;
