/**
 *  Return _v_ as `T` if the passed _v_ is not `null`.
 *  Otherwise, throw `TypeError` with the passed `msg`.
 */
export function expectNotNull(v, msg) {
    if (v === null) {
        throw new TypeError(msg);
    }
    return v;
}
