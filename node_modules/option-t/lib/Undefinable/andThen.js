"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var Undefinable_1 = require("./Undefinable");
/**
 *  Returns `undefined` if the _src_ is `undefined`,
 *  otherwise calls _selector_ with the value and returns the result.
 *
 *  XXX:
 *  Some languages call this operation flatmap.
 *  But we don't provide `flatMap()` as alias of this function.
 *  because it's too hard to undarstand that "flatMap" operation for `T | undefined`
 */
function andThenForUndefinable(src, selector) {
    if (Undefinable_1.isNotUndefined(src)) {
        var r = selector(src);
        return r;
    }
    else {
        return src;
    }
}
exports.andThenForUndefinable = andThenForUndefinable;
