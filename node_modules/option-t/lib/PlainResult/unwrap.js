"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var expect_1 = require("./expect");
/**
 *  Return the inner `T` of a `Ok(T)`.
 *
 *  @throws {TypeError}
 *      Throws if the self is a `Err`.
 */
function unwrapFromResult(v) {
    return expect_1.expectIsOk(v, 'called with `Err`');
}
exports.unwrapFromResult = unwrapFromResult;
/**
 *  Return the inner `E` of a `Err(E)`.
 *
 *  @throws {TypeError}
 *      Throws if the self is a `Ok`.
 */
function unwrapErrFromResult(v) {
    return expect_1.expectIsErr(v, 'called with `Ok`');
}
exports.unwrapErrFromResult = unwrapErrFromResult;
