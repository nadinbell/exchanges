/**
 *  Return _v_ as `T` if the passed _v_ is `Some(T)`.
 *  Otherwise, return the result of _def_.
 */
export function orElseForOption(v, def) {
    if (v.ok) {
        return v;
    }
    else {
        var r = def();
        return r;
    }
}
