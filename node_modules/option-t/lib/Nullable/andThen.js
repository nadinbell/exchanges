"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var Nullable_1 = require("./Nullable");
/**
 *  Returns `null` if the _src_ is `null`,
 *  otherwise calls _selector_ with the value and returns the result.
 *
 *  XXX:
 *  Some languages call this operation flatmap.
 *  But we don't provide `flatMap()` as alias of this function.
 *  because it's too hard to undarstand that "flatMap" operation for `T | null`
 */
function andThenForNullable(src, selector) {
    if (Nullable_1.isNotNull(src)) {
        var r = selector(src);
        return r;
    }
    else {
        return src;
    }
}
exports.andThenForNullable = andThenForNullable;
